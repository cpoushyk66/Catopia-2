import React from "react";
import styled from "styled-components";

export function Logo({sideLength}) {

    return (
        <img src="../assets/images/Hotpot2.png" width={`${sideLength} px`} height={`${sideLength} px`}/>
    )
}